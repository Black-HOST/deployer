name: FTP

on:
  push:
    branches: [ "master" ]

jobs:
  ftps_sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Full Sync to FTP Server
        uses: ./
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          port: "21"
          protocol: "ftp"
          local_dir: ".github/artifacts"
          remote_dir: "/public_html/FTPS/sync"
          secure: "true"
          verify_tls: "true"
          passive: "true"
          delete: "true"
          only_newer: "false"
          parallel: "2"
          exclude: ".git,node_modules,*.log"
          dry_run: "false"